#include "main.h"

/** 
 * _abs - check the code
 * @n: a function that computes the absolute value of integer
 * Return: always 0. 
 */ 

int _abs(int n)

{
	if( n > 0)
	{
		return (n); 
	}
	else
	{
		return (-n);
	}

	return (0); 
}
